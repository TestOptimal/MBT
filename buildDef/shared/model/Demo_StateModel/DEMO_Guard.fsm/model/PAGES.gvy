// Page script (page object definition) for UI pages, elements and actions.
import org.openqa.selenium.By;

// Examples:
// mainPage = $PAGEMGR.addPage('MainPage');
// mainPage.addAction ('getBodyText', { page ->
//     $SELENIUM.getWebDriver().findElement(By.tagName('body')).getText();
// })
// 
// elemBalance = mainPage.addElement('Balance', By.id('amount'))
// elemBalance.addAction('getText', { elem, params ->
//     return $SELENIUM.getWebDriver().findElement(elem.locator).getText();
// })
