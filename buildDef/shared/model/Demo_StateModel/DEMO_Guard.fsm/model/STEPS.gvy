// Step script - step implementation
import com.testoptimal.exec.mscript.STEP
//Example (in TRIGGERS script, call myCheckAndRecordStep with '>> Check 123 and record abc'): 
//@STEP('Check {p1} and record {p2}')
//static void myCheckAndRecordStep(p1, p2) {
//   print(p1 + ',' + p2)
//}
